// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: agents/v1/worker.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";

export const protobufPackage = "agents.v1";

export interface SubscribeRequest {
  workerId: string;
  capabilities: string[];
}

export interface JobAssignment {
  jobId: string;
  agentId: string;
  activityType: string;
  paramsJson: string;
  payloadJson: string;
  stepId: string;
  contextJson: string;
  stepLabel: string;
}

export interface JobResult {
  jobId: string;
  agentId: string;
  success: boolean;
  resultJson: string;
  error: string;
  stepId: string;
  logsJson: string;
}

export interface JobAck {
  accepted: boolean;
}

function createBaseSubscribeRequest(): SubscribeRequest {
  return { workerId: "", capabilities: [] };
}

export const SubscribeRequest: MessageFns<SubscribeRequest> = {
  encode(message: SubscribeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workerId !== "") {
      writer.uint32(10).string(message.workerId);
    }
    for (const v of message.capabilities) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubscribeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscribeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.capabilities.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubscribeRequest {
    return {
      workerId: isSet(object.workerId)
        ? globalThis.String(object.workerId)
        : isSet(object.worker_id)
        ? globalThis.String(object.worker_id)
        : "",
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: SubscribeRequest): unknown {
    const obj: any = {};
    if (message.workerId !== "") {
      obj.workerId = message.workerId;
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubscribeRequest>, I>>(base?: I): SubscribeRequest {
    return SubscribeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubscribeRequest>, I>>(object: I): SubscribeRequest {
    const message = createBaseSubscribeRequest();
    message.workerId = object.workerId ?? "";
    message.capabilities = object.capabilities?.map((e) => e) || [];
    return message;
  },
};

function createBaseJobAssignment(): JobAssignment {
  return {
    jobId: "",
    agentId: "",
    activityType: "",
    paramsJson: "",
    payloadJson: "",
    stepId: "",
    contextJson: "",
    stepLabel: "",
  };
}

export const JobAssignment: MessageFns<JobAssignment> = {
  encode(message: JobAssignment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    if (message.agentId !== "") {
      writer.uint32(18).string(message.agentId);
    }
    if (message.activityType !== "") {
      writer.uint32(26).string(message.activityType);
    }
    if (message.paramsJson !== "") {
      writer.uint32(34).string(message.paramsJson);
    }
    if (message.payloadJson !== "") {
      writer.uint32(42).string(message.payloadJson);
    }
    if (message.stepId !== "") {
      writer.uint32(50).string(message.stepId);
    }
    if (message.contextJson !== "") {
      writer.uint32(58).string(message.contextJson);
    }
    if (message.stepLabel !== "") {
      writer.uint32(66).string(message.stepLabel);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobAssignment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobAssignment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.agentId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.activityType = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.paramsJson = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payloadJson = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.stepId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.contextJson = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.stepLabel = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobAssignment {
    return {
      jobId: isSet(object.jobId)
        ? globalThis.String(object.jobId)
        : isSet(object.job_id)
        ? globalThis.String(object.job_id)
        : "",
      agentId: isSet(object.agentId)
        ? globalThis.String(object.agentId)
        : isSet(object.agent_id)
        ? globalThis.String(object.agent_id)
        : "",
      activityType: isSet(object.activityType)
        ? globalThis.String(object.activityType)
        : isSet(object.activity_type)
        ? globalThis.String(object.activity_type)
        : "",
      paramsJson: isSet(object.paramsJson)
        ? globalThis.String(object.paramsJson)
        : isSet(object.params_json)
        ? globalThis.String(object.params_json)
        : "",
      payloadJson: isSet(object.payloadJson)
        ? globalThis.String(object.payloadJson)
        : isSet(object.payload_json)
        ? globalThis.String(object.payload_json)
        : "",
      stepId: isSet(object.stepId)
        ? globalThis.String(object.stepId)
        : isSet(object.step_id)
        ? globalThis.String(object.step_id)
        : "",
      contextJson: isSet(object.contextJson)
        ? globalThis.String(object.contextJson)
        : isSet(object.context_json)
        ? globalThis.String(object.context_json)
        : "",
      stepLabel: isSet(object.stepLabel)
        ? globalThis.String(object.stepLabel)
        : isSet(object.step_label)
        ? globalThis.String(object.step_label)
        : "",
    };
  },

  toJSON(message: JobAssignment): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    if (message.agentId !== "") {
      obj.agentId = message.agentId;
    }
    if (message.activityType !== "") {
      obj.activityType = message.activityType;
    }
    if (message.paramsJson !== "") {
      obj.paramsJson = message.paramsJson;
    }
    if (message.payloadJson !== "") {
      obj.payloadJson = message.payloadJson;
    }
    if (message.stepId !== "") {
      obj.stepId = message.stepId;
    }
    if (message.contextJson !== "") {
      obj.contextJson = message.contextJson;
    }
    if (message.stepLabel !== "") {
      obj.stepLabel = message.stepLabel;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JobAssignment>, I>>(base?: I): JobAssignment {
    return JobAssignment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobAssignment>, I>>(object: I): JobAssignment {
    const message = createBaseJobAssignment();
    message.jobId = object.jobId ?? "";
    message.agentId = object.agentId ?? "";
    message.activityType = object.activityType ?? "";
    message.paramsJson = object.paramsJson ?? "";
    message.payloadJson = object.payloadJson ?? "";
    message.stepId = object.stepId ?? "";
    message.contextJson = object.contextJson ?? "";
    message.stepLabel = object.stepLabel ?? "";
    return message;
  },
};

function createBaseJobResult(): JobResult {
  return { jobId: "", agentId: "", success: false, resultJson: "", error: "", stepId: "", logsJson: "" };
}

export const JobResult: MessageFns<JobResult> = {
  encode(message: JobResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    if (message.agentId !== "") {
      writer.uint32(18).string(message.agentId);
    }
    if (message.success !== false) {
      writer.uint32(24).bool(message.success);
    }
    if (message.resultJson !== "") {
      writer.uint32(34).string(message.resultJson);
    }
    if (message.error !== "") {
      writer.uint32(42).string(message.error);
    }
    if (message.stepId !== "") {
      writer.uint32(50).string(message.stepId);
    }
    if (message.logsJson !== "") {
      writer.uint32(58).string(message.logsJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.agentId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.resultJson = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.error = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.stepId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.logsJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobResult {
    return {
      jobId: isSet(object.jobId)
        ? globalThis.String(object.jobId)
        : isSet(object.job_id)
        ? globalThis.String(object.job_id)
        : "",
      agentId: isSet(object.agentId)
        ? globalThis.String(object.agentId)
        : isSet(object.agent_id)
        ? globalThis.String(object.agent_id)
        : "",
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      resultJson: isSet(object.resultJson)
        ? globalThis.String(object.resultJson)
        : isSet(object.result_json)
        ? globalThis.String(object.result_json)
        : "",
      error: isSet(object.error) ? globalThis.String(object.error) : "",
      stepId: isSet(object.stepId)
        ? globalThis.String(object.stepId)
        : isSet(object.step_id)
        ? globalThis.String(object.step_id)
        : "",
      logsJson: isSet(object.logsJson)
        ? globalThis.String(object.logsJson)
        : isSet(object.logs_json)
        ? globalThis.String(object.logs_json)
        : "",
    };
  },

  toJSON(message: JobResult): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    if (message.agentId !== "") {
      obj.agentId = message.agentId;
    }
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.resultJson !== "") {
      obj.resultJson = message.resultJson;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    if (message.stepId !== "") {
      obj.stepId = message.stepId;
    }
    if (message.logsJson !== "") {
      obj.logsJson = message.logsJson;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JobResult>, I>>(base?: I): JobResult {
    return JobResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobResult>, I>>(object: I): JobResult {
    const message = createBaseJobResult();
    message.jobId = object.jobId ?? "";
    message.agentId = object.agentId ?? "";
    message.success = object.success ?? false;
    message.resultJson = object.resultJson ?? "";
    message.error = object.error ?? "";
    message.stepId = object.stepId ?? "";
    message.logsJson = object.logsJson ?? "";
    return message;
  },
};

function createBaseJobAck(): JobAck {
  return { accepted: false };
}

export const JobAck: MessageFns<JobAck> = {
  encode(message: JobAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobAck {
    return { accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false };
  },

  toJSON(message: JobAck): unknown {
    const obj: any = {};
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JobAck>, I>>(base?: I): JobAck {
    return JobAck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobAck>, I>>(object: I): JobAck {
    const message = createBaseJobAck();
    message.accepted = object.accepted ?? false;
    return message;
  },
};

export type WorkerServiceDefinition = typeof WorkerServiceDefinition;
export const WorkerServiceDefinition = {
  name: "WorkerService",
  fullName: "agents.v1.WorkerService",
  methods: {
    subscribeToJobs: {
      name: "SubscribeToJobs",
      requestType: SubscribeRequest,
      requestStream: false,
      responseType: JobAssignment,
      responseStream: true,
      options: {},
    },
    reportJobResult: {
      name: "ReportJobResult",
      requestType: JobResult,
      requestStream: false,
      responseType: JobAck,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface WorkerServiceImplementation<CallContextExt = {}> {
  subscribeToJobs(
    request: SubscribeRequest,
    context: CallContext & CallContextExt,
  ): ServerStreamingMethodResult<DeepPartial<JobAssignment>>;
  reportJobResult(request: JobResult, context: CallContext & CallContextExt): Promise<DeepPartial<JobAck>>;
}

export interface WorkerServiceClient<CallOptionsExt = {}> {
  subscribeToJobs(
    request: DeepPartial<SubscribeRequest>,
    options?: CallOptions & CallOptionsExt,
  ): AsyncIterable<JobAssignment>;
  reportJobResult(request: DeepPartial<JobResult>, options?: CallOptions & CallOptionsExt): Promise<JobAck>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export type ServerStreamingMethodResult<Response> = { [Symbol.asyncIterator](): AsyncIterator<Response, void> };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
