services:
    postgres:
        image: postgres:17-alpine
        environment:
            POSTGRES_DB: agents
            POSTGRES_USER: agents
            POSTGRES_PASSWORD: agents
        ports:
            - '5444:5432'
        volumes:
            - agents-pgdata:/var/lib/postgresql/data

volumes:
    agents-pgdata:
